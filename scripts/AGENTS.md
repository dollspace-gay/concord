# Verification and operator tooling

Keep stable executable entry points here. Subdirectories own suite orchestration, architecture inspection, and load/recovery support.

Required suites must fail on missing prerequisites or empty test filters. Preserve secret redaction, bounded smoke/full distinctions, cleanup, and source provenance when moving helper code.

Run affected script self-tests and suites. python3 scripts/check-maintainability.py verifies the source budget and folder guidance.
