# Browser source

Components consume stores and API adapters. Stores own client state and command correlation; transport validation stays in api/.

Keep asynchronous callbacks bound to their captured account and protected generation. Do not reintroduce shared mutable view state or unchecked external URLs.

Use frontend lint/build and the relevant harness/authenticated browser journeys.
