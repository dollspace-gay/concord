# Integration findings during implementation

This is a working integration record, not the final security or correctness review. The full design remains controlling. A reported focused pass does not close a release gate; final evidence must identify the completed source and executed checks.

| ID | Owner | Finding and required outcome | Current evidence/state |
| --- | --- | --- | --- |
| I01 | migration_foundation, irc_streams | Revalidate queued private payloads at final WS/IRC delivery; close/resynchronize on count or byte overflow. Cover query responses and ordinary fanout. | Guard sidecar and 256-event/1-MiB limits implemented; focused tests reported. Complete sensitive-event coverage and queued-revocation socket proof remain to verify. |
| I02 | irc_streams | Snapshot and replay payloads must fit queue limits. Bound messages, reaction details and bytes with a usable continuation for populated accounts. | Message window and compact reaction counts implemented; oversized-state continuation and browser convergence remain to verify. |
| I03 | irc_streams | A missed immediate fanout must be recovered by a supervised durable dispatcher, using indexed eligible subscribers and current authorized projections. | Wakeup/poll dispatcher and recovery test reported. Final source/load verification pending. |
| I04 | irc_streams | Never expose global event sequence on the wire. Preserve a contiguous completion frontier and pending gaps during retention. | Raw DurableEvent global sequence removed in inspected source; bounded pruning/gap tests reported. |
| I05 | irc_streams | Credential database faults must propagate as retryable dependency failures, not credential revocation that causes outbox completion without delivery. | Returned for explicit auth-error classification and credential-lookup fault regression. |
| I06 | irc_streams | Database restore generation must not make backed-up canonical receipts invisible to retries. Expired client-operation generations must be rejected. | Returned for separate cursor/operation generation semantics, handshake fields and restore/expiry tests. |
| I07 | irc_streams | Kill the canonical transaction before commit and after commit before receipt emission; recover without partial state or duplicate retry. Reap owned probe children on runner failure. | Parent inspected feature-gated canonical BeforeCommit/AfterCommit barriers and cleanup trap, then executed the runner successfully: `/tmp/concord-remediation-storage-faults-canonical.log`. Link/event/receipt/lock faults also passed. Separate storage-sync/power-loss evidence remains outside this process-crash proof. |
| I08 | irc_streams | Remove the old production detached message mutation path and exercise meaningful regressions through the canonical service. | Legacy path restricted to test builds; canonical regression coverage remains part of F01–F08 completion. |
| I09 | migration_foundation, irc_streams | Stable reserved IRC nicknames independent of long web handles; no offline nickname impersonation; RFC1459 collisions; real per-user defaults and channel aliases. | Identity/resolution APIs landing; IRC adapter consumption and complete transport tests in progress. |
| I10 | migration_foundation | Aggregate presence from live connections and persisted requested status. Preserve invisible/DND/custom state, avoid stale online after crash and false quit on one of several connections. | Requested-status schema and connection projection being implemented; complete restart/multi-client proof pending. |
| I11 | migration_foundation | Actor-scoped transactional organization/moderation/community services must bind every resource to its actual parent. Topic/channel creation cannot acknowledge detached writes. | S6 implementation in progress. Event cross-server mutation and legacy topic/join paths explicitly identified. |
| I12 | verification_contracts | Media upload/download must support canonical DMs and private threads. Profile/server media must use the same Actor policy as profile/server projections, including bans and scopes. | Managed media intents landing; route-level access matrix and final download-start checks pending. |
| I13 | verification_contracts | Media filesystem containment must cover parent symlinks, GC and import. Stale import attempts must never overwrite newer accepted bytes before losing a DB fence. | Rooted capability operations and immutable per-attempt objects implemented; changed-content stale-worker regression reported passing. Parent combined verification pending. |
| I14 | verification_contracts | Historical imports must stream/hash within their own byte/concurrency budget and renew/claim leases at worker capacity. Valid files above generic metadata response limits must import. | Streaming, incremental hashing and throttled fenced lease renewals implemented; large-body and fragmented-body tests reported passing. Full importer scheduling/operational evidence remains. |
| I15 | verification_contracts | Managed asset claims and updates revalidate actors inside the mutation transaction. Distinguish member avatars from server icons. | Routes being implemented; revoke/role-race and per-member intent tests pending. |
| I16 | verification_contracts | Job attempts need unique claim fencing, not only a reusable worker name. Stale completion/failure cannot affect a replacement lease. | Fencing and real database-reopen dispatch tests reported passing; production integration dispatch remains S7. |
| I17 | irc_streams | Live browser must consume generated DTO validation and durable synchronization, correlate/retry pending commands, clear revoked caches, preserve drafts and support DMs. | Generated artifacts and existing frontend build pass at a bounded checkpoint; runtime/store/UI integration pending. |
| I18 | irc_streams, migration_foundation | Share the 128-pending-write/overall-five-second admission budget across domain services, including pool checkout and lock acquisition. Preserve cancellation safety and bounded telemetry. | Messaging deadline tests reported; common coordinator and S6 consumption in progress. |
| I19 | migration_foundation | Announcement lineage must survive grant removal/source deletion sufficiently for audit and reconciliation; pending work cancels without silently erasing mappings. | Migration 024 cascade issue returned; complete F19 implementation pending. |
| I20 | verification_contracts | Migrate and rotate recoverable provider/signing/pending OAuth keys, persist pending flows, and fail closed on missing/corrupt established keys. | Encrypted credential foundation reported passing; operator lifecycle and controlled OAuth fixtures in progress. |
| I21 | all implementation owners | Required suite runners must execute full journeys/faults rather than label foundation tests as complete qualification. | Migration/container/foundation job runners exist; application-policy deliberately remains incomplete; browser, packaging/restore and load suites remain required. |
| I22 | verification_contracts | Bound upload concurrency and idle/total request duration. Persist a collectable reservation before asynchronous file creation; throttle progress writes and fence stale GC/import attempts. | Four-permit admission and 15s/300s defaults implemented; 14 media tests reported passing, including cancellation and fragment bounds. Router stalled-body test added; combined verification in progress. |
| I23 | migration_foundation, irc_streams | The IRC second line queue must preserve authorization evidence to the actual socket write. Socket writes need cancellation/deadlines; sensitive event coverage cannot rely on JSON field-name inference. | Parent verified remaining second-queue gap and returned it. Typed event guard coverage and held-writer revocation tests underway. |
| I24 | irc_streams | Retained receipt identity is unique by principal/client message ID across operation epochs. New epochs cannot resurrect retained or expired uncertain operations. Epoch rotation itself uses safe coordinated writes. | Parent inspected migration-025 unique principal/client index, retained-receipt lookup before epoch validation and shared admitted rotation before replay read transactions. Owner reports rollover/restore/expiry regressions passing; parent reran canonical crash suite successfully at schema 25. Browser retry behavior remains to verify. |
| I25 | migration_foundation | Domain error types preserve authentication, hidden-resource denial, invalid input and retryable dependency distinctions to the adapter boundary. | OrganizationService String errors returned for typed conversion. |
| I26 | verification_contracts | Outbound requests share bounded admission and per-origin capacity, with reserved OAuth/administrative capacity. Public safe redirects transfer permits under a total deadline; operator private destinations require an integration-scoped allowlist. | Shared EgressServices classes implemented; per-origin/redirect/allowlist qualification remains in progress. |
| I27 | verification_contracts | A timeout after a remote mutation must not cause blind create retry. Refresh classification and account serialization precede remote refresh; deterministic publication identity and reconciliation resolve uncertain outcomes. | Parent returned unconditional PDS refresh/retry and concurrent one-use refresh-token races for S7 completion. |
| I28 | irc_streams | Generated schema, handshake and browser use one protocol version. Executable fixtures cover all variants, and browser snapshot/replay handlers apply projections before advancing the cursor. | Runtime generated validators and epoch bootstrap landing. Parent found schema version 1 versus handshake version 2 and thin synchronization handlers; full implementation remains required. |
| I29 | migration_foundation, irc_streams | Channel identifiers and IRC serialization reject line-control injection while preserving mutable display names and stable transport aliases. | Parent returned permissive channel/topic/control-character validation for explicit rules and transport regressions. |
| I30 | irc_streams | Deferred retries remain bound to the originating account and connection. Logout/account replacement cannot send a captured private command through a new user's socket. | Owner reports account-generation/socket binding, timer cancellation and browser race regression passing. Full authenticated multi-tab journey remains. |
| I31 | irc_streams | Legacy immediate notifications and durable v2 delivery cannot duplicate messages/unread counts or overwrite newer entity versions/tombstones. | Parent found unconditional legacy append/edit handlers alongside durable upserts; both delivery orders and stale events returned for correction. |
| I32 | irc_streams | Server-triggered revocation and denied reconnects clear protected projections while preserving account-scoped drafts. | Parent returned generic disconnected status retaining private caches after credential/permission-driven socket close. Real socket/browser proof required. |
| I33 | irc_streams | Message deletion does not restore rate or slow-mode capacity. Accepted-send accounting spans connections and adapters. | Parent found `deleted_at IS NULL` in channel rate, slow-mode and direct-send rate queries; send/delete regressions required. |
| I34 | verification_contracts | Operator key rotation persists recoverable replacement bytes before DB commit, and maintenance excludes all servers sharing the actual DB identity. Recovery commands remain usable with lost provider keys. | Durable key copies/directory sync and exclusion implemented; shared URL/DB-alias locking helper plus encrypted-state crash/activation tests remain under verification. |

| I35 | migration_foundation | Thread archive/unarchive cache and event ordering must respect committed state. A delayed automatic sweep cannot overwrite a later unarchive; durable versions/recovery preserve convergence. | Parent found unconditional postcommit cache writes; owner adding versioned projection and overlap tests. |
| I36 | migration_foundation | Private and privileged events are initially routed only to authorized recipients, with final writer guards retained for later revocation. Ordinary viewers must not be disconnected whenever another member creates a private thread or performs moderation. | Parent found parent-wide private-thread and server-wide privileged event enqueue followed by writer denial/disconnection; recipient filtering returned. |

| I37 | irc_streams | Full compositions, including pending files/voice/reply context, remain bound to their originating account and conversation across asynchronous uploads and server rejection. Socket enqueue success is not canonical acceptance; failed/uncertain messages need recoverable state and accurate rendering. | Parent found post-upload send using the current global socket, component-wide file state, partial-upload loss, ignored voice/GIF send failure and rejected optimistic rows surviving after their pending commands are removed. Component/account-switch/server-rejection journeys returned. |

| I38 | migration_foundation | Historical inventory and audited repair run against each actual source schema, including blocked v14–16. Unknown schema and ambiguous identity mappings must fail unchanged; deterministic notification reconciliation preserves full original rows before uniqueness is installed. | Parent caught preflight referencing a v19 column at v9, then passed repaired source1–16 matrix and all12 repair tests. Owner added real operator CLI workflow and migration027; combined verification underway. |
| I39 | verification_contracts | Webhook/integration lifecycle creation, update and deletion bind current Actor authority, resource scope, principal, installation, credential and audit inside one admitted transaction; live cancellation follows commit. | Canonical execute route reported passing. Parent found lifecycle check-then-write and multi-commit cleanup/revoke/delete, returned for IntegrationService and transaction-taking credential APIs plus fault tests. |
| I40 | verification_contracts | Publication create/edit/delete/reconcile uses the stored collection and deterministic record key, coherent versioned grants, durable uncertain-outcome handling, lease-safe ordering and terminal user-visible status. | Parent found edit trigger omits grant version, delete hardcodes another collection, missing remote URI incorrectly cancels uncertain deletion, sequential preleased batches lack renewal, and terminal job failures leave publication pending. Returned for complete dispatcher and provider-fault fixtures. |
| I41 | migration_foundation | Forum tag mutations preserve moderator-only add/remove authority, actual thread ownership, typed failures and auditable/versioned convergence. | Parent found selected-tags-only moderation check permits removal of an existing moderated tag, parent-message author is substituted for unpersisted thread creator, SQL/auth failures collapse to hidden denial, and selection changes lack audit/versioned recovery. Returned for full F11 corrections and regression journeys. |
| I42 | verification_contracts | Refresh credentials remain bound to their verified grant issuer/token endpoint, and concurrent reauthentication reloads one coherent token/key/PDS context. | Parent found refresh rediscovers an arbitrary new issuer from mutable PDS metadata without a persisted original grant origin; on credential version change it returns a new token while the caller retains the old DPoP key and PDS URL. Returned for persisted origin binding and provider/concurrent-reauth fixtures. |
| I43 | irc_streams | Authenticated application mount/server/DM transitions preserve React hook ordering and fail verification on uncaught runtime errors. | Parent actual browser diagnostic run received server and direct-conversation events, then exposed ChannelList's new DM early return before useEffect/useMemo/useCallback. React tore down the app and sockets. The HTTP-only smoke still passed, so UI health assertions and pageerror failure are required alongside the component fix. |
| I44 | verification_contracts | Outgoing webhook enqueue and final dispatch respect the configured channel, current source eligibility and versioned destination grant; external identities remain opaque and exact signatures are verified. | Parent found server-wide enqueue ignoring w.channel_id, dispatcher lacking source checks, outgoing channel changes without grant invalidation, global event sequence exposed in payload and only header-presence signature assertions. Owner confirmed and is adding canonical send-to-local-receiver scope/revocation/deletion and exact HMAC tests. Internal event-sequence dedup remains valid. |
| I45 | irc_streams | Direct sends reuse the existing stable conversation ID for a participant pair and never create an orphan or split history on a pair conflict. | The actual-browser fixture used a valid stored non-derived pair ID. Parent found send_direct unconditionally derives a new ID then ignores the pair uniqueness conflict. Returned for lookup-first transactional resolution and retaining that fixture as a regression, not merely renaming its ID. |
| I46 | irc_streams | Repeated or older read commands do not manufacture state advancement, and delayed read projections preserve newer unread messages using sequence and stable principal identity. | Parent found mark_read always increments version/emits read_advanced after a no-op MAX update; browser blindly zeroes unread on any read projection and compares mutable nicknames for self detection. The live UI also repeatedly submits reads on message-array changes. Returned for end-to-end monotone/idempotent read handling and reordered-event tests. |
| I47 | irc_streams / verification_contracts | Bounded connection admission supports authenticated users/tabs sharing an IP and the specified reconnect target while retaining preauthentication/global abuse protection. | Combined authenticated tests hit the hardcoded production WS burst of five attempts per IP with 12-second refill. Parent returned authenticated principal/credential admission and same-IP/multi-tab/reconnect proof; isolating tests or spoofing forwarding headers cannot substitute for required production behavior. |
| I48 | migration_foundation | Template creation and instantiation preserve hidden configuration boundaries, coherent authorized snapshots and safe remapping. | Parent found creation exports all cached private names/topics after a server-level permission check, swallows role/category read failures, and mutates outside the admitted actor transaction. Instantiation needs only source VIEW_CHANNELS and makes that member owner of a copy containing hidden private configuration. Returned for explicit publication/access policy, typed transactional lifecycle and private-denied journey coverage. |
| I49 | migration_foundation | Bounded ban cleanup deletes only the originally authorized message range and uses the canonical deletion effects. | Parent found the new job has no upper acceptance boundary, so delayed cleanup after unban can delete later sends; its duplicate tombstone implementation omits announcement propagation and outgoing delete events. Selection and remaining-work queries also disagree on channel eligibility. Returned for immutable range, shared transaction-taking deletion logic and restart/unban/side-effect regressions. |
| I50 | irc_streams, with F13/F25 owners | Pending HTTP responses cannot repopulate protected data after account or authorization changes; authentication checks cannot resurrect a logged-out account. | Parent found emoji/sticker/identity/AT-setting loaders write unconditionally after await and AuthStore checkAuth can race logout. Account generation currently does not advance for every protected-view reset. Returned for shared generation/abort discipline and held-response account/revocation journeys, including component-local integration responses. |

| I51 | irc_streams | Reply projections enforce current same-conversation scope and deletion redaction, including historically malformed references and reload/replay. | Parent found durable reply lookup selects by target ID alone. Canonical new-send validation cannot protect a legacy public reply pointing to a private target. Returned for read-time conversation binding and seeded historical snapshot/event regression; the legacy history lookup also needs explicit deletion redaction. |

| I52 | verification_contracts | Backup/restore is bounded, refuses overlapping source/destination paths, preserves integer operation epochs and cannot activate a partial original-generation restore. | Parent found restore inserts datetime text into integer operation epochs, copies backup files into active paths before generation/session/job rewrite with no enforced activation guard, backup beneath the media root recursively copies itself, and file copying reads each complete database/media file into RAM. Returned for actual post-restore send/replay/auth canary, staged fail-closed activation/recovery, interrupted-restore fixtures, canonical path exclusion and streaming copy. |

| I53 | migration_foundation, with irc_streams for shared browser state | Server lifecycle operations revalidate the actual credential inside admitted transactions, and authoritative server/channel removal clears protected projections and repairs active navigation. | Parent found OrganizationService provision/delete receive only owner user IDs, so REST/WS credential revocation while waiting cannot be rechecked; related update/join/leave/admin-delete paths require the same audit. Browser server_list only replaces the server array, retaining removed-server caches and stale active navigation; channel removal also omits several secondary cached copies. Returned for Actor-bound lifecycle/fault tests and actual active-server deletion with usable surviving navigation. |

| I54 | migration_foundation | Invite, community, scheduled-event and announcement lifecycle mutations bind current Actor, actual resource scope and graph validation inside admitted transactions. | Parent confirmed DeleteInvite authorizes a supplied server but deletes any supplied invite ID; create/update/follow paths also retain check-then-write authority, and reciprocal follows can race the out-of-transaction cycle check. Returned within F17–F19 completion for typed CommunityService lifecycle, foreign-resource preservation, revoked-actor and concurrent-cycle regressions. |

| I55 | verification_contracts, with irc_streams for the browser facade | Slash/component invocation revalidates Actor, source message/expiry, installed handler and action schema in the admitted insertion transaction; the browser waits for correlated outcomes and preserves rejected input. | Parent found current invocation checks and create_interaction run separately, while the slash composer clears text after void dispatch and components use a one-second pending timer. Returned for transactional invocation and actual acceptance/error states, stable empty selectors, valid optional/single-select values and duplicate-value rejection. |

| I56 | irc_streams | Desktop notifications apply structured targets, role/everyone suppression, true channel-over-server inheritance and account DND before bounded cross-tab arbitration. | Parent found localStorage get/set/get claim can race across tabs, grows per-message keys without cleanup and falls back to duplicates; its test repeats sequentially in one page. Current policy also ignores role targets, treats default as mentions and bypasses server-only DND for DMs. Returned within F04 for full policy and real concurrent-tab evidence. |

| I57 | verification_contracts, with irc_streams for shared preview policy | Automatic rich-embed media uses the controlled private-media/preview policy or an explicit reader opt-in, with bounded URLs/payloads; clickable links have a separate policy. | Parent found the new rich renderer automatically fetches arbitrary HTTPS image/icon/thumbnail URLs in the reader browser, while server validation checks only HTTPS and host presence. Returned for safe asset delivery or explicit external-image controls, URL/userinfo/bounds validation and reader-side negative evidence. |

| I58 | migration_foundation | Folder structural saves are ordered and account-bound with visible pending/failure/retry, and hydration cannot overwrite a newer local edit; collapse state stays local. | Parent found new folder persistence dispatches unordered whole-layout PUTs, suppresses every failure and also PUTs on local collapse. Returned for serialized/coalesced structural state, generation/edit guards, explicit recovery and held GET/PUT/account-transition regressions. |

| I59 | migration_foundation, with irc_streams for role-notification consumers | Role metadata and assignments hydrate for authorized viewers/self and converge on live changes, deletion and reconnect; role colors and structured role mentions use that authoritative state. | Parent found WS role update/delete/assign/remove events are sent only to the requester, MemberInfo has no role IDs and no assignment bootstrap is present. Owner-only management tests therefore miss stale peer colors and absent ordinary-member role notifications. Returned for scoped hydration/fanout and real peer/reconnect evidence. |

| I60 | verification_contracts | Delegated OAuth consent, code exchange, refresh, scoped access and revocation use current credential/application/target authority and atomic failures; public PKCE clients require no secret. Consent identifies the publisher and resource scope, and exact redirect binding survives the complete lifecycle. | Parent returned consent check-then-write, public-client secret requirement and ignored refresh-family revocation failures. Initial repairs are present; current lifecycle completion must also cover disabled users, changed client credentials/targets, grant versions, exact redirect comparison and final scoped reads. Actual issuer endpoint/fault evidence remains required. |

| I61 | verification_contracts, with irc_streams for socket/bootstrap recovery | HTTP and command admission support legitimate authenticated bootstrap/reconnect workloads while preserving bounded verification/abuse control; temporary dependency failure presents recoverable sign-in state. | Full authenticated suite reached API 429 responses: all validated REST requests still first consume the 60-per-IP anonymous budget, preventing later app bootstrap despite the larger credential budget. Initial verification failure displays a sign-in screen without retry. The same run emitted uncorrelated WebSocket command-rate errors during repeated bootstrap requests. Returned for real middleware/concurrent same-IP evidence, bounded command admission and request/recovery behavior. |

| I62 | irc_streams, with migration_foundation for shared authorization | Search preserves typed failures and globally authorized counts/order while bounding policy work and SQL parameters; pagination and pending responses converge with message edits/deletion. | Parent found search reads every server channel and builds an unbounded authorized-channel IN list; the ChatEngine facade collapses every policy/database error to one String denial. Returned within full F07 completion with large-channel, pagination and reordered-result/tombstone evidence required. |

| I63 | irc_streams, with migration_foundation for typed message identities | Historical and canonical message IDs/timestamps remain stable through every history/reply/replay adapter, including non-UUID IDs and recognized SQLite/RFC3339 timestamp forms. | Parent observed the same history message receiving a different timestamp on later reads. The channel history projector parses SQLite-space timestamps as RFC3339 and falls back to the current time; it also parses stored message IDs into UUID with a default fallback. Returned for shared validated identity/time conversion and real browser/IRC non-UUID and mixed-format persistence fixtures. |
| I64 | verification_contracts | Bluesky profile synchronization resolves the authenticated account's linked DID and commits a current-Actor-authorized coherent profile update after provider I/O, preserving unrelated fields and safe media ownership. | Parent found the route still assumes local user ID equals DID, performs separate unadmitted writes, ignores avatar/bio failures and replaces managed media with provider URLs. Returned for the retained F25 profile surface, including stable-local-ID, held-response revocation, failure atomicity and live projection cases. |
| I65 | irc_streams | The declared FULL durability profile has separate storage write/sync failure evidence, with immutable fault-probe executable provenance and no acknowledged partial mutation. | Current storage-faults runner covers process SIGKILL, SQL trigger failures and lock timeout. Those are useful but do not exercise SQLite/filesystem sync failure. Returned for an isolated storage-I/O fault harness, restart integrity and canonical retry checks without weakening production synchronization. |
| I66 | verification_contracts, coordinated with domain owners | Architecture checks enforce the completed actor-scoped ownership boundary across HTTP, WebSocket and IRC, including imported repository aliases; ChatEngine remains a forwarding facade for migrated use cases. | The first check guards twelve selected WebSocket arms and engine import direction only. REST and remaining adapters still have unrestricted domain SQL paths; moderation.rs initially contains only an error type. Returned for actual scoped service ownership and comprehensive checks, with narrowly justified authentication/storage adapter exceptions rather than whole-file exemptions. |
| I67 | integrations_completion | The full load/recovery runner implements the specified mixed transport, dataset, fanout, history/search, reconnect, abusive-client, media/provider and resource workloads and verifies every acceptance invariant against the measured host. | First generator uses plaintext IRC on one fixed channel, has a source-IP inventory/index mismatch, reads no sender acknowledgements or heartbeat, measures first peer receipt, permits unexplained missing recipients and ignores client errors/resource samples. Full acceptance is incorrectly claimed from those narrow checks. The runner also copies raw credential inventory into evidence before redacting it. Returned for a complete workload, TLS, canonical acceptance/recovery accounting, actual resource validation and secret-free evidence construction. |

| I68 | integrations_completion | Stopped-service credential recovery revokes the complete declared local credential scope atomically, including delegated access/refresh authority and pending issuance, while permitting safe revocation for disabled accounts. | Parent found the new CredentialRevokeAll updates only auth_credentials; locally issued oauth2_tokens/grants remain usable through the issuer. Its shared admin-promotion helper also rejects disabled targets for revocation. Returned for coherent revocation/audit and actual access/refresh plus rollback evidence. |

| I69 | integrations_completion and community_completion, with messaging_completion for shared request outcomes | Lifecycle controls track correlated acceptance, preserve rejected input, bound duplicate submissions and isolate credentials/actions to their originating account/context. | Parent found BotsTab, OAuthTab, CreateServerModal and InvitesTab clear or close creation forms immediately after void dispatch, while moderation/install/remove/token controls expose no request-bound pending/error state. Returned within full feature UI completion with a shared correlated success/error mechanism and actual rejection/disconnect/retry journeys; non-idempotent CRUD must not be blindly replayed. |

| I70 | integrations_completion | Container packaging includes the same operator lifecycle tools as source installation and verifies their use against the stopped service storage. | Parent found Dockerfile builds/copies only concord-server, omits concord_operator and the new build.rs input. Returned for the installed operator artifact, matching mount/configuration documentation and an actual container operator invocation after shutdown. |

| I71 | messaging_completion | Readiness and metrics remain bounded and owned across timeout/caller cancellation, protect operator diagnostics, and measure actual failures/recovery. | Parent found random readiness probe files can escape cleanup when the outer timeout drops their future; repeated anonymous metrics scrapes launch independent multi-table counts and expose instance content/session/job totals. Returned for cancellation-safe bounded probe ownership/cleanup, bounded scrape admission and an authorized monitoring boundary. Retained-row gauges do not replace commit/queue/replay/worker failure instrumentation. |

| I72 | community_completion, coordinated with integrations_completion | Dependency qualification updates affected dependencies where compatible and records exact enabled-feature/call-path reachability for remaining unpatched advisories, preserving provider/transport behavior. | Parent fresh cargo audit reports 14 vulnerability matches plus unsound/unmaintained/yanked warnings (`/tmp/concord-remediation-cargo-audit-schema32.json`, RustSec database commit 5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5). Returned for patch/parent updates and focused authentication/provider/TLS verification before the final source freeze. Current hickory/lru defaults are re-enabled by atproto-oauth's identity dependency; the DNSSEC feature is absent. Concord's JWT path uses explicit HS256 and secret keys, not the unpatched RSA private-key operations. These observations are partial triage, not an audit pass. |

Later required work still includes every uncompleted F01–F26 journey, full bot/application/publication integrations, operator backup/restore and recovery, documentation, observability, and qualification. The Daybreak Blue security review starts after coding completion, followed by Carmilla's final correctness review and any resulting repairs.
