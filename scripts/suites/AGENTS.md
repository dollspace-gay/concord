# Required suite adapters

Each executable selects and checks one qualification area through stable repository-root paths.

Test filters must execute nonempty suites. When moving tests, update both selectors and prerequisite source checks. Preserve existing failure propagation and acceptance scope.

Run the affected suite through scripts/run-required-suite.sh and check shell syntax.
