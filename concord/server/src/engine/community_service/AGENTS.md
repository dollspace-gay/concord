# Community service service

The parent community_service.rs owns shared service types and its public interface; child modules own cohesive domain operations.

Preserve invite redemption limits, membership/rules checks, event authorization, announcement routing, and template snapshot validation.

Run the community_service unit tests and affected application-policy or transport journeys, plus strict Rust checks.
