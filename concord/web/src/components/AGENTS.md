# React views

Components render store state and invoke typed actions. Keep reusable accessibility and external-content safeguards shared.

Preserve labels, focus restoration, keyboard controls, IME behavior, and visible error/retry feedback. Move a tab, renderer, or hook by responsibility; avoid oversized prop bags for unrelated state.

Run frontend lint/build plus harness tests for changed views and authenticated journeys for server-backed actions.
